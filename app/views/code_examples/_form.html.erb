<%= form_for(@code_example) do |f| %>
  <% if @code_example.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_example.errors.count, "error") %> prohibited this code_example from being saved:</h2>

      <ul>
      <% @code_example.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => "form-control" %>
  </div>
  <br>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, :class => "form-control",cols: "30", rows: "15" %>
  </div>
  <br>
  <div class="action">
    <%= f.submit %>
  </div>
<% end %>
